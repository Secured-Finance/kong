########################################
# redis module — Redis runs on GCE instance (see gce module)
########################################
# Redis is now hosted on the GCE instance alongside ingest service
# No Memorystore instance needed
# This module is intentionally empty but kept for future expansion
