########################################
# redis.tf — Redis runs on GCE instance (see gce.tf)
########################################
# Redis is now hosted on the GCE instance alongside ingest service
# No Memorystore instance needed
